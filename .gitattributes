# Normalización de fin de línea
* text=auto

# Fuerza LF en scripts de shell
*.sh text eol=lf

# Trata estos tipos como binarios
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.mp4 binary
*.wav binary
*.mp3 binary
*.ogg binary
*.ttf binary
*.otf binary

# (Opcional) Git LFS para assets grandes (requiere instalar Git LFS)
# Descomenta si usarás LFS:
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text